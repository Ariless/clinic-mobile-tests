{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "names=$(ls .claude/skills | tr '\\n' ' '); /opt/homebrew/bin/jq -Rns --arg names \"$names\" '{hookSpecificOutput:{hookEventName:\"UserPromptSubmit\",additionalContext:(\"PROJECT SKILLS — before writing or editing any page object, step definition, selector or API call, load the matching skill from .claude/skills/ via the Skill tool. Load every skill that applies, not just one. Available: \"+$names)}}'",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
